package org.dbrain.yaw.http;

/**
 * Created by epoitras on 10/09/14.
 */
public class HttpServerTest {


    @org.junit.Test
    public void testHttpServer() throws Exception {
//        URI serverUrl = new URI( "http://localhost:8080" );
//
//        ServletContextBuilder servletContext = new ServletContextBuilder( "/" );
//
//        servletContext.serve( ServletDef.of( "/*", new SampleServlet() ) )
//                      .serve( WebSocketDef.of( SampleWebSocket.class ) );
//
//        Server server = new HttpServerBuilder( serverUrl ) //
//                .setupSsl( getClass().getResource( "/keystore.jks" ).toURI(), "password", "password" )
//                .addServletContext( servletContext.build() )
//                .build();
//        server.start();
//        server.dump( System.out );
//
////        server.join();
//
//        // TODO Load the stream here since there is nothing in the URL api to do that.
//        Object content = new URL( "http://localhost:8080/" ).getContent();
//        Assert.assertEquals( content.toString(), SampleServlet.CONTENT );
//        // TODO Check that the server serves stuff here
    }
}
